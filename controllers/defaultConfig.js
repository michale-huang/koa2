const config = {
    database: {
        HOST: 'localhost', //服务器ip
        DATABASE: 'user', //数据库名称
        USERNAME: 'root', //mysql用户名
        PASSWORD: 'admin' //mysql密码
    }
}

module.exports = config
